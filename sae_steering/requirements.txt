# Extra dependencies for the SAE topic-steering experiments (macOS / MPS friendly).
# Install into the project venv:  uv pip install -r sae_steering/requirements.txt
#
# transformer-lens gives named activation hooks; sae-lens loads Gemma Scope SAEs
# and pulls a compatible transformer-lens automatically.
torch>=2.5.0
transformer-lens>=2.0.0
sae-lens>=5.0.0
transformers>=4.46.0
sentencepiece>=0.2.0
